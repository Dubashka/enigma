# Phase 2: Detection + Masking - Context

**Gathered:** 2026-03-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Движок детекции чувствительных колонок по ключевым словам в названиях + полный движок маскирования (текстовые псевдонимы с авто-префиксом, числовые коэффициенты, кросс-листовая консистентность). UI для выбора колонок (Шаг 2) и превью результата (Шаг 3). Выгрузка файлов и дешифровка — в Phase 3.

</domain>

<decisions>
## Implementation Decisions

### Детекция чувствительных колонок
- Жёсткий список ключевых слов, зашитый в код (константы/конфиг)
- Детекция только по названию колонки, не по значениям ячеек (детекция по значениям — v2, DETC-05)
- Подстрочный поиск: если название содержит ключевое слово — колонка отмечена
- Если ни одна колонка не найдена — показать все колонки без предвыбора + предупреждение "Автоматически чувствительные колонки не обнаружены, выберите вручную"
- Список ключевых слов не редактируется пользователем через UI

### UI выбора колонок (Шаг 2)
- Табы по листам (как на Шаге 1), на каждом листе свои чекбоксы
- Рядом с каждым чекбоксом бейдж типа маскирования: [текст] или [число]
- Кнопки "Выбрать все" / "Снять все" на каждом листе
- Автоматически определённые колонки предвыбраны (checked)
- Тип маскирования определяется автоматически по dtype колонки + эвристика для идентификаторов

### Классификация числовых колонок (MASK-04)
- По ключевым словам в названии: №, номер, договор, контракт, документ, счёт, id → идентификатор (текстовое маскирование)
- Остальные числовые колонки → количественные (коэффициент)
- Для числовых колонок пользователь может переключить тип: коэффициент ↔ идентификатор
- Текстовые колонки всегда маскируются как текст (без возможности смены типа)

### Числовое маскирование
- Один случайный коэффициент (0.5–1.5) на колонку — пропорции между строками сохраняются
- Коэффициент единый кросс-листово для одноимённых колонок

### Результат маскирования (Шаг 3)
- Превью замаскированных данных (20 строк, табы по листам — аналогично Шагу 1)
- Статистика: количество замаскированных значений и уникальных сущностей
- Заглушки кнопок скачивания (неактивные) — Phase 3 активирует
- Кнопка "Назад к выбору колонок" для перемаскирования
- Кнопка "Сбросить" — начать с загрузки

### Обработка пустых значений
- NaN/пустые ячейки пропускаются — не маскируются, не попадают в маппинг

### Claude's Discretion
- Полный список ключевых слов для детекции (на основе реального файла)
- Визуальное оформление бейджей типа маскирования
- Порядок отображения колонок в чекбоксах (как в файле vs отсортировано)
- Дизайн переключателя "коэффициент ↔ идентификатор" для числовых колонок

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project context
- `.planning/PROJECT.md` — Видение продукта, ключевые решения (авто-префикс, коэффициент, кросс-листовый маппинг)
- `.planning/REQUIREMENTS.md` — Требования DETC-01..03, MASK-01..04 для этой фазы
- `.planning/research/ARCHITECTURE.md` — Архитектура: единый маппинг-словарь до обхода листов, BytesIO
- `.planning/research/PITFALLS.md` — Подводные камни: openpyxl RAM, числа-идентификаторы, Unicode нормализация

### Phase 1 code (integration points)
- `core/parser.py` — `parse_upload()` возвращает `dict[str, DataFrame]` — входные данные для детекции
- `core/state_keys.py` — Ключи session state: STAGE_COLUMNS, STAGE_MASKED уже определены
- `views/masking.py` — Шаг 1 (загрузка) + Шаг 2 (превью) — нужно расширить для Шагов 2 и 3
- `ui/step_indicator.py` — Индикатор шагов, уже поддерживает 3 шага

### Sample data
- `Данные для маскирования_13.03.xlsx` — Реальный тестовый файл с 4 листами, жёлтые колонки = чувствительные

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `core/parser.py:parse_upload()` — уже возвращает `dict[str, DataFrame]`, прямой вход для детекции и маскирования
- `core/state_keys.py` — STAGE_COLUMNS и STAGE_MASKED уже определены, готовы к использованию
- `ui/upload_widget.py:render_preview()` — уже умеет показывать табы по листам с 20 строками, можно переиспользовать для превью результата
- `ui/step_indicator.py` — 3-шаговый индикатор, поддерживает подсветку текущего шага

### Established Patterns
- Session state stage machine: `None → uploaded → columns_selected → masked`
- Табы по листам для многолистовых файлов (st.tabs)
- Кнопки "Сбросить" / "Далее" / "Назад" на каждом шаге
- Русскоязычные сообщения об ошибках

### Integration Points
- `views/masking.py` — расширить для стадий `columns_selected` и `masked`
- Кнопка "Далее" на Шаге 1 (`_render_step_preview`) — сейчас disabled=True, нужно активировать
- Session state: добавить ключи для selected_columns, masked_sheets, mapping, statistics

</code_context>

<specifics>
## Specific Ideas

- Тестировать на реальном файле "Данные для маскирования_13.03.xlsx" — проверить детекцию на реальных названиях колонок
- Псевдоним формируется из названия колонки: "Предприятие A", "Предприятие B" (не "Компания A")
- Одно уникальное значение = один псевдоним кросс-листово (ООО "ЛУИС+" → "Предприятие A" на всех листах)

</specifics>

<deferred>
## Deferred Ideas

- DETC-04: Детекция по жёлтой заливке ячеек (v2)
- DETC-05: Детекция по значениям (ООО/ОАО/ИП regex, ФИО паттерны) (v2)
- Редактируемый пользователем список ключевых слов через UI

</deferred>

---

*Phase: 02-detection-masking*
*Context gathered: 2026-03-19*
