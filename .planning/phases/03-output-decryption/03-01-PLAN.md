---
phase: 03-output-decryption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/output.py
  - core/decryptor.py
  - tests/test_output.py
  - tests/test_decryptor.py
autonomous: true
requirements: [OUT-01, OUT-02, OUT-03, DECR-01, DECR-02, DECR-03]

must_haves:
  truths:
    - "generate_masked_xlsx produces valid xlsx bytes containing all input sheets"
    - "generate_mapping_json produces UTF-8 JSON with text and numeric keys"
    - "generate_mapping_xlsx produces xlsx with two sheets: Текстовый маппинг, Числовой маппинг"
    - "decrypt_sheets restores pseudonyms to normalized originals via inverted text mapping"
    - "decrypt_sheets divides numeric columns by their coefficient"
    - "Unknown values and columns (LLM-added) pass through unchanged"
    - "NaN cells remain NaN after decryption"
  artifacts:
    - path: "core/output.py"
      provides: "Output generation functions"
      exports: ["generate_masked_xlsx", "generate_mapping_json", "generate_mapping_xlsx"]
    - path: "core/decryptor.py"
      provides: "Decryption engine"
      exports: ["decrypt_sheets", "load_mapping_json"]
    - path: "tests/test_output.py"
      provides: "Unit tests for output generators"
      min_lines: 40
    - path: "tests/test_decryptor.py"
      provides: "Unit tests for decryption engine"
      min_lines: 60
  key_links:
    - from: "core/output.py"
      to: "core/masker.py"
      via: "consumes mapping dict format {text: {norm_val: pseudonym}, numeric: {col: multiplier}}"
      pattern: "mapping\\[.text.\\]|mapping\\[.numeric.\\]"
    - from: "core/decryptor.py"
      to: "core/masker.py"
      via: "inverts mapping[text] dict for reverse lookup"
      pattern: "v:\\s*k\\s+for\\s+k,\\s*v\\s+in\\s+mapping"
---

<objective>
Create the pure-logic output generation functions and decryption engine with full TDD test coverage.

Purpose: All download and decryption logic must be tested independently before wiring into the Streamlit UI. These are pure functions with no Streamlit dependency — they take dicts/DataFrames and return bytes/dicts.
Output: `core/output.py`, `core/decryptor.py`, `tests/test_output.py`, `tests/test_decryptor.py` — all tests green.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-output-decryption/03-CONTEXT.md
@.planning/phases/03-output-decryption/03-RESEARCH.md

@core/masker.py
@core/parser.py
@core/state_keys.py

<interfaces>
<!-- Mapping dict format (contract from masker.py) -->
From core/masker.py:mask_sheets():
```python
# Returns (masked_sheets, mapping, stats) where:
# mapping = {"text": {norm_val: pseudonym}, "numeric": {col: multiplier}}
# Example:
# mapping = {
#   "text": {"ООО АЛЬФА": "Предприятие A", "ООО БЕТА": "Предприятие B"},
#   "numeric": {"Сумма": 1.234}
# }
```

From core/parser.py:
```python
def parse_upload(uploaded_file) -> dict[str, pd.DataFrame]:
    """Parse uploaded file bytes into dict[sheet_name, DataFrame]."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Output generators with TDD (OUT-01, OUT-02, OUT-03)</name>
  <files>core/output.py, tests/test_output.py</files>
  <read_first>core/masker.py, core/parser.py</read_first>
  <behavior>
    - test_masked_xlsx_has_all_sheets: generate_masked_xlsx({"Sheet1": df1, "Sheet2": df2}) returns bytes that, when read back via pd.read_excel(BytesIO(result), sheet_name=None), contain both "Sheet1" and "Sheet2" with matching row counts
    - test_masked_xlsx_preserves_columns: generated xlsx has same column names as input DataFrames
    - test_mapping_json_format: generate_mapping_json({"text": {"X": "Y"}, "numeric": {"Col": 1.2}}) returns bytes decodable as UTF-8 JSON with keys "text" and "numeric"
    - test_mapping_json_cyrillic: generate_mapping_json with Cyrillic values ({"text": {"ООО АЛЬФА": "Предприятие A"}}) returns bytes containing literal Cyrillic characters (not \u escapes)
    - test_mapping_xlsx_two_sheets: generate_mapping_xlsx returns xlsx with sheet names "Текстовый маппинг" and "Числовой маппинг"
    - test_mapping_xlsx_text_columns: "Текстовый маппинг" sheet has columns ["Оригинал", "Псевдоним"]
    - test_mapping_xlsx_numeric_columns: "Числовой маппинг" sheet has columns ["Колонка", "Коэффициент"]
  </behavior>
  <action>
RED phase: Create tests/test_output.py with all 7 tests above. Use concrete test data:
- For xlsx tests: create two DataFrames — df1 = pd.DataFrame({"Name": ["A", "B"], "Value": [1, 2]}), df2 = pd.DataFrame({"City": ["X"]})
- For JSON tests: mapping = {"text": {"ООО АЛЬФА": "Предприятие A", "ООО БЕТА": "Предприятие B"}, "numeric": {"Сумма": 1.234}}
- For mapping xlsx tests: same mapping dict
- Run tests — they must all FAIL (module core.output does not exist)

GREEN phase: Create core/output.py with three functions:

```python
import io
import json
import pandas as pd

def generate_masked_xlsx(masked_sheets: dict[str, pd.DataFrame]) -> bytes:
    buf = io.BytesIO()
    with pd.ExcelWriter(buf, engine="xlsxwriter") as writer:
        for sheet_name, df in masked_sheets.items():
            df.to_excel(writer, sheet_name=sheet_name, index=False)
    buf.seek(0)
    return buf.read()

def generate_mapping_json(mapping: dict) -> bytes:
    return json.dumps(mapping, indent=2, ensure_ascii=False).encode("utf-8")

def generate_mapping_xlsx(mapping: dict) -> bytes:
    buf = io.BytesIO()
    with pd.ExcelWriter(buf, engine="xlsxwriter") as writer:
        text_df = pd.DataFrame(
            list(mapping.get("text", {}).items()),
            columns=["Оригинал", "Псевдоним"],
        )
        text_df.to_excel(writer, sheet_name="Текстовый маппинг", index=False)
        numeric_df = pd.DataFrame(
            list(mapping.get("numeric", {}).items()),
            columns=["Колонка", "Коэффициент"],
        )
        numeric_df.to_excel(writer, sheet_name="Числовой маппинг", index=False)
    buf.seek(0)
    return buf.read()
```

Run tests — all 7 must PASS.
  </action>
  <verify>
    <automated>.venv2/bin/python -m pytest tests/test_output.py -v</automated>
  </verify>
  <acceptance_criteria>
    - core/output.py contains `def generate_masked_xlsx(`
    - core/output.py contains `def generate_mapping_json(`
    - core/output.py contains `def generate_mapping_xlsx(`
    - core/output.py contains `ensure_ascii=False`
    - core/output.py contains `engine="xlsxwriter"`
    - core/output.py contains `sheet_name="Текстовый маппинг"`
    - core/output.py contains `sheet_name="Числовой маппинг"`
    - core/output.py contains `columns=["Оригинал", "Псевдоним"]`
    - core/output.py contains `columns=["Колонка", "Коэффициент"]`
    - tests/test_output.py contains at least 7 test functions (grep `def test_` count >= 7)
    - `.venv2/bin/python -m pytest tests/test_output.py -v` exits 0
  </acceptance_criteria>
  <done>All 7 output tests pass. generate_masked_xlsx produces valid multi-sheet xlsx. generate_mapping_json produces UTF-8 Cyrillic JSON. generate_mapping_xlsx produces xlsx with two correctly-named sheets and columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decryption engine with TDD (DECR-01, DECR-02, DECR-03)</name>
  <files>core/decryptor.py, tests/test_decryptor.py</files>
  <read_first>core/masker.py, core/output.py</read_first>
  <behavior>
    - test_load_mapping_valid: load_mapping_json(BytesIO with valid JSON bytes) returns dict with "text" and "numeric" keys
    - test_load_mapping_invalid: load_mapping_json(BytesIO with b"not json") returns None
    - test_decrypt_text_values: decrypt_sheets with mapping {"text": {"ООО АЛЬФА": "Предприятие A"}} and df containing "Предприятие A" returns df with "ООО АЛЬФА"
    - test_decrypt_numeric_values: decrypt_sheets with mapping {"numeric": {"Сумма": 1.5}} and df with Сумма=150 returns df with Сумма=100.0
    - test_decrypt_nan_passthrough: NaN cells in both text and numeric columns remain NaN after decryption
    - test_decrypt_unknown_values_passthrough: cell value "New Corp" not in reverse map stays "New Corp"
    - test_decrypt_unknown_columns_passthrough: column "LLM Added" not in mapping stays unchanged
    - test_decrypt_multi_sheet: decrypt_sheets handles dict with 2 sheets correctly
    - test_decrypt_integer_rounding: numeric column with integer dtype masked by 1.5 decrypts back to integer (Int64)
  </behavior>
  <action>
RED phase: Create tests/test_decryptor.py with all 9 tests. Use concrete test data:
- mapping = {"text": {"ООО АЛЬФА": "Предприятие A", "ООО БЕТА": "Предприятие B"}, "numeric": {"Сумма": 1.5}}
- For text test: df with column "Контрагент" containing ["Предприятие A", "Предприятие B", None]
- For numeric test: df with column "Сумма" containing [150.0, 300.0, None]
- For passthrough test: df with extra column "LLM Added" containing ["yes", "no"] and text cell "New Corp" not in mapping
- For load_mapping tests: use io.BytesIO wrapping json.dumps(mapping).encode("utf-8") for valid, and io.BytesIO(b"not json") for invalid
- Run tests — all must FAIL

GREEN phase: Create core/decryptor.py with two functions:

```python
from __future__ import annotations
import json
import pandas as pd

def load_mapping_json(uploaded_file) -> dict | None:
    try:
        content = uploaded_file.read()
        mapping = json.loads(content if isinstance(content, str) else content.decode("utf-8"))
        if "text" not in mapping or "numeric" not in mapping:
            return None
        return mapping
    except (json.JSONDecodeError, UnicodeDecodeError):
        return None

def decrypt_sheets(
    sheets: dict[str, pd.DataFrame],
    mapping: dict,
) -> dict[str, pd.DataFrame]:
    reverse_text = {v: k for k, v in mapping.get("text", {}).items()}
    numeric_map = mapping.get("numeric", {})

    result = {}
    for sheet_name, df in sheets.items():
        decrypted = df.copy()
        for col in decrypted.columns:
            if col in numeric_map:
                coeff = numeric_map[col]
                series = decrypted[col]
                divided = series / coeff
                if pd.api.types.is_integer_dtype(series):
                    decrypted[col] = divided.round().astype("Int64")
                else:
                    decrypted[col] = divided.round(2)
            else:
                decrypted[col] = decrypted[col].map(
                    lambda v, rt=reverse_text: rt.get(str(v), v) if pd.notna(v) else v
                )
        result[sheet_name] = decrypted
    return result
```

Run tests — all 9 must PASS.
  </action>
  <verify>
    <automated>.venv2/bin/python -m pytest tests/test_decryptor.py -v</automated>
  </verify>
  <acceptance_criteria>
    - core/decryptor.py contains `def load_mapping_json(`
    - core/decryptor.py contains `def decrypt_sheets(`
    - core/decryptor.py contains `reverse_text = {v: k for k, v in`
    - core/decryptor.py contains `series / coeff`
    - core/decryptor.py contains `astype("Int64")`
    - tests/test_decryptor.py contains at least 9 test functions (grep `def test_` count >= 9)
    - `.venv2/bin/python -m pytest tests/test_decryptor.py -v` exits 0
    - `.venv2/bin/python -m pytest tests/ -x -q` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>All 9 decryptor tests pass. Text decryption restores pseudonyms to normalized originals. Numeric decryption divides by coefficient. NaN passthrough works. Unknown values/columns from LLM pass through unchanged. Full test suite green.</done>
</task>

</tasks>

<verification>
- `.venv2/bin/python -m pytest tests/test_output.py tests/test_decryptor.py -v` — all 16+ tests green
- `.venv2/bin/python -m pytest tests/ -x -q` — full suite green (including existing Phase 1 and 2 tests)
- `grep -c "def test_" tests/test_output.py` returns >= 7
- `grep -c "def test_" tests/test_decryptor.py` returns >= 9
</verification>

<success_criteria>
- core/output.py exists with 3 exported functions: generate_masked_xlsx, generate_mapping_json, generate_mapping_xlsx
- core/decryptor.py exists with 2 exported functions: load_mapping_json, decrypt_sheets
- All unit tests pass (16+ tests across 2 test files)
- Full test suite remains green
- No Streamlit imports in core/output.py or core/decryptor.py (pure logic only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-decryption/03-01-SUMMARY.md`
</output>
