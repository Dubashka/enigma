---
phase: 03-output-decryption
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - views/masking.py
  - views/decryption.py
  - core/state_keys.py
autonomous: false
requirements: [OUT-01, OUT-02, OUT-03, OUT-04, DECR-01, DECR-02, DECR-03]

must_haves:
  truths:
    - "User can download masked xlsx file from masking results page"
    - "User can download JSON mapping file from masking results page"
    - "User can download Excel mapping file from masking results page"
    - "Statistics (masked values count, unique entities) are visible on Step 3"
    - "User can upload masked file + JSON mapping on decryption page"
    - "User sees preview of masked data before decrypting"
    - "User clicks Decrypt and sees restored data preview"
    - "User can download decrypted xlsx file"
    - "Decryption page works independently from masking flow (own state)"
  artifacts:
    - path: "views/masking.py"
      provides: "Download buttons replacing stubs"
      contains: "st.download_button"
    - path: "views/decryption.py"
      provides: "Full decryption page with upload, preview, decrypt, download"
      min_lines: 50
    - path: "core/state_keys.py"
      provides: "Decryption state keys"
      contains: "DECR_RESULT"
  key_links:
    - from: "views/masking.py"
      to: "core/output.py"
      via: "import generate_masked_xlsx, generate_mapping_json, generate_mapping_xlsx"
      pattern: "from core\\.output import"
    - from: "views/decryption.py"
      to: "core/decryptor.py"
      via: "import decrypt_sheets, load_mapping_json"
      pattern: "from core\\.decryptor import"
    - from: "views/decryption.py"
      to: "core/parser.py"
      via: "import parse_upload for masked file parsing"
      pattern: "from core\\.parser import"
---

<objective>
Wire output generators into the masking results page (replace stub buttons with real st.download_button) and build the full decryption page UI.

Purpose: Connect the tested pure functions from Plan 01 to Streamlit UI — users can download masked files/mappings and restore original data via decryption.
Output: Updated `views/masking.py` with working download buttons, full `views/decryption.py` with upload+preview+decrypt+download flow, updated `core/state_keys.py`.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-output-decryption/03-CONTEXT.md
@.planning/phases/03-output-decryption/03-RESEARCH.md
@.planning/phases/03-output-decryption/03-01-SUMMARY.md

@views/masking.py
@views/decryption.py
@core/state_keys.py
@core/output.py
@core/decryptor.py
@core/parser.py
@ui/upload_widget.py

<interfaces>
<!-- From Plan 01 output: core/output.py -->
```python
def generate_masked_xlsx(masked_sheets: dict[str, pd.DataFrame]) -> bytes: ...
def generate_mapping_json(mapping: dict) -> bytes: ...
def generate_mapping_xlsx(mapping: dict) -> bytes: ...
```

<!-- From Plan 01 output: core/decryptor.py -->
```python
def load_mapping_json(uploaded_file) -> dict | None: ...
def decrypt_sheets(sheets: dict[str, pd.DataFrame], mapping: dict) -> dict[str, pd.DataFrame]: ...
```

<!-- Existing: core/parser.py -->
```python
def parse_upload(uploaded_file) -> dict[str, pd.DataFrame]: ...
```

<!-- Existing: ui/upload_widget.py -->
```python
def render_preview(sheets: dict[str, pd.DataFrame]) -> None: ...
```

<!-- Existing: core/state_keys.py -->
```python
SHEETS = "sheets"
STAGE = "stage"
FILE_NAME = "file_name"
STAGE_MASKED = "masked"
MASKED_SHEETS = "masked_sheets"
MAPPING = "mapping"
STATS = "stats"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace download stubs with real st.download_button (OUT-01, OUT-02, OUT-03) + add decryption state keys</name>
  <files>views/masking.py, core/state_keys.py</files>
  <read_first>views/masking.py, core/state_keys.py, core/output.py</read_first>
  <action>
1. In core/state_keys.py, add three new keys at the bottom:

```python
# Phase 3 keys — decryption page
DECR_SHEETS = "decr_sheets"      # dict[str, pd.DataFrame] — uploaded masked file
DECR_MAPPING = "decr_mapping"    # dict — loaded JSON mapping
DECR_RESULT = "decr_result"      # dict[str, pd.DataFrame] — decrypted sheets
```

2. In views/masking.py, add import at top (after existing imports):

```python
from core.output import generate_masked_xlsx, generate_mapping_json, generate_mapping_xlsx
```

3. In views/masking.py, in function `_render_step_masked()`, replace lines 155-159 (the four lines with disabled buttons and caption):

```python
    # Download stubs (Phase 3 will implement)
    st.button("Скачать замаскированный файл", disabled=True, use_container_width=True)
    st.button("Скачать маппинг (JSON)", disabled=True, use_container_width=True)
    st.button("Скачать маппинг (Excel)", disabled=True, use_container_width=True)
    st.caption("Будет доступно в следующей версии")
```

with:

```python
    # --- Download buttons ---
    mapping = st.session_state[MAPPING]
    base_name = file_name.rsplit(".", 1)[0] if "." in file_name else file_name

    st.download_button(
        label="Скачать замаскированный файл",
        data=generate_masked_xlsx(masked_sheets),
        file_name=f"{base_name}_masked.xlsx",
        mime="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        use_container_width=True,
    )
    st.download_button(
        label="Скачать маппинг (JSON)",
        data=generate_mapping_json(mapping),
        file_name=f"{base_name}_mapping.json",
        mime="application/json",
        use_container_width=True,
    )
    st.download_button(
        label="Скачать маппинг (Excel)",
        data=generate_mapping_xlsx(mapping),
        file_name=f"{base_name}_mapping.xlsx",
        mime="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        use_container_width=True,
    )
```

Do NOT change any other part of _render_step_masked(). The st.metric statistics (OUT-04) remain as-is from Phase 2.
  </action>
  <verify>
    <automated>grep -c "st.download_button" views/masking.py | grep -q "3" && grep "DECR_RESULT" core/state_keys.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - views/masking.py contains exactly 3 occurrences of `st.download_button`
    - views/masking.py contains `from core.output import generate_masked_xlsx, generate_mapping_json, generate_mapping_xlsx`
    - views/masking.py contains `file_name=f"{base_name}_masked.xlsx"`
    - views/masking.py contains `file_name=f"{base_name}_mapping.json"`
    - views/masking.py contains `file_name=f"{base_name}_mapping.xlsx"`
    - views/masking.py does NOT contain `disabled=True` on download buttons
    - views/masking.py does NOT contain "Будет доступно в следующей версии"
    - core/state_keys.py contains `DECR_SHEETS = "decr_sheets"`
    - core/state_keys.py contains `DECR_MAPPING = "decr_mapping"`
    - core/state_keys.py contains `DECR_RESULT = "decr_result"`
    - `.venv2/bin/python -m pytest tests/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Three working st.download_button calls replace the disabled stubs. File names follow pattern {original_name}_masked.xlsx, {original_name}_mapping.json, {original_name}_mapping.xlsx. Decryption state keys added. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Build full decryption page (DECR-01, DECR-02, DECR-03)</name>
  <files>views/decryption.py</files>
  <read_first>views/decryption.py, core/decryptor.py, core/parser.py, ui/upload_widget.py, core/state_keys.py, views/masking.py</read_first>
  <action>
Replace the entire contents of views/decryption.py with a full decryption page. The page must:

1. Be completely independent from the masking flow — uses its own session state keys (DECR_SHEETS, DECR_MAPPING, DECR_RESULT), never reads MASKED_SHEETS or MAPPING.

2. Structure:

```python
import streamlit as st
import pandas as pd

from core.parser import parse_upload
from core.decryptor import load_mapping_json, decrypt_sheets
from core.output import generate_masked_xlsx
from core.state_keys import DECR_SHEETS, DECR_MAPPING, DECR_RESULT
from ui.upload_widget import render_preview


def render() -> None:
    st.header("Дешифровка данных")
    st.markdown(
        "Загрузите замаскированный файл и JSON-маппинг, чтобы восстановить оригинальные значения."
    )

    col_file, col_json = st.columns(2)
    with col_file:
        uploaded_file = st.file_uploader(
            "Замаскированный файл (xlsx или csv)",
            type=["xlsx", "csv"],
            key="decr_file_uploader",
        )
    with col_json:
        uploaded_json = st.file_uploader(
            "Файл маппинга (JSON)",
            type=["json"],
            key="decr_json_uploader",
        )

    if uploaded_file is not None and uploaded_json is not None:
        # Parse masked file
        try:
            sheets = parse_upload(uploaded_file)
        except ValueError as e:
            st.error(str(e))
            return

        # Load mapping
        mapping = load_mapping_json(uploaded_json)
        if mapping is None:
            st.error("Невалидный файл маппинга. Убедитесь, что это JSON-файл с ключами 'text' и 'numeric'.")
            return

        st.session_state[DECR_SHEETS] = sheets
        st.session_state[DECR_MAPPING] = mapping

        st.subheader("Замаскированные данные")
        render_preview(sheets)

        if st.button("Дешифровать", type="primary", use_container_width=True):
            result = decrypt_sheets(sheets, mapping)
            st.session_state[DECR_RESULT] = result

    # Show decrypted result if available
    if DECR_RESULT in st.session_state:
        st.subheader("Восстановленные данные")
        result = st.session_state[DECR_RESULT]
        render_preview(result)

        # Derive download filename from uploaded file
        decr_file_name = "decrypted.xlsx"
        if uploaded_file is not None:
            base = uploaded_file.name.rsplit(".", 1)[0] if "." in uploaded_file.name else uploaded_file.name
            decr_file_name = f"{base}_decrypted.xlsx"

        st.download_button(
            label="Скачать восстановленный файл",
            data=generate_masked_xlsx(result),
            file_name=decr_file_name,
            mime="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            use_container_width=True,
        )

        if st.button("Сбросить", use_container_width=True):
            for key in [DECR_SHEETS, DECR_MAPPING, DECR_RESULT]:
                st.session_state.pop(key, None)
            st.rerun()
```

Key design points:
- Two file uploaders side by side in columns
- Error handling for invalid JSON (st.error with descriptive Russian message)
- Preview of masked data before decryption
- "Дешифровать" button triggers decrypt_sheets
- Decrypted result stored in session state and shown with render_preview
- Download button uses generate_masked_xlsx (same Excel generator — it takes any dict[str, DataFrame])
- Download filename: {original_name}_decrypted.xlsx
- "Сбросить" button clears decryption state
- No imports of MASKED_SHEETS, MAPPING, or STATS — fully independent
  </action>
  <verify>
    <automated>grep -c "st.file_uploader" views/decryption.py | grep -q "2" && grep "decrypt_sheets" views/decryption.py && grep "DECR_RESULT" views/decryption.py && grep "st.download_button" views/decryption.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - views/decryption.py contains exactly 2 occurrences of `st.file_uploader`
    - views/decryption.py contains `from core.decryptor import load_mapping_json, decrypt_sheets`
    - views/decryption.py contains `from core.output import generate_masked_xlsx`
    - views/decryption.py contains `from core.parser import parse_upload`
    - views/decryption.py contains `from ui.upload_widget import render_preview`
    - views/decryption.py contains `from core.state_keys import DECR_SHEETS, DECR_MAPPING, DECR_RESULT`
    - views/decryption.py contains `st.download_button`
    - views/decryption.py contains `_decrypted.xlsx`
    - views/decryption.py does NOT contain `MASKED_SHEETS` or import it
    - views/decryption.py does NOT contain `MAPPING` (the masking-flow key, not the local variable)
    - views/decryption.py contains `st.error` (error handling for invalid mapping)
    - views/decryption.py is at least 50 lines
    - `.venv2/bin/python -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Full decryption page: two file uploaders, masked data preview, decrypt button, restored data preview, download xlsx button. Page is fully independent from masking flow — uses own session state keys. Invalid JSON mapping shows error. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of download + decryption flow</name>
  <files>views/masking.py, views/decryption.py</files>
  <action>Human visual verification of the complete output and decryption flow. No code changes — verify that Tasks 1 and 2 work correctly in the browser.</action>
  <what-built>
    Complete output and decryption flow:
    1. Three download buttons on masking results page (masked xlsx, JSON mapping, Excel mapping)
    2. Full decryption page with file upload, preview, decrypt, and download
  </what-built>
  <how-to-verify>
    1. Run: `.venv2/bin/python -m streamlit run app.py`
    2. **Test downloads (masking page):**
       - Upload "Данные для маскирования_13.03.xlsx"
       - Select columns, click "Замаскировать"
       - On Step 3 (results page), verify statistics are shown (st.metric)
       - Click "Скачать замаскированный файл" — verify xlsx downloads with all sheets
       - Click "Скачать маппинг (JSON)" — open file, verify it has "text" and "numeric" keys with Cyrillic values
       - Click "Скачать маппинг (Excel)" — open file, verify two sheets "Текстовый маппинг" and "Числовой маппинг"
    3. **Test decryption page:**
       - Switch to "Дешифровка" in sidebar
       - Upload the masked xlsx file + the JSON mapping file you just downloaded
       - Verify masked data preview appears
       - Click "Дешифровать"
       - Verify restored data preview appears with original values (in UPPERCASE — this is expected, normalized form)
       - Click "Скачать восстановленный файл" — verify xlsx downloads
    4. **Test error handling:**
       - On decryption page, upload a non-JSON file as mapping — verify Russian error message appears
    5. **Test independence:**
       - Open decryption page directly (without going through masking first) — verify it works standalone
  </how-to-verify>
  <verify>Human visual verification — see how-to-verify steps above</verify>
  <done>All download buttons produce correct files. Decryption page works end-to-end. Error handling shows Russian messages. Page works independently from masking flow.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `.venv2/bin/python -m pytest tests/ -v` — full suite green
- Three download buttons visible on masking Step 3 page (no disabled stubs)
- Decryption page functional with two uploaders, preview, decrypt, download
- End-to-end: mask file -> download masked xlsx + JSON -> upload both to decryption -> download restored file
</verification>

<success_criteria>
- Download buttons produce correct files (xlsx with all sheets, JSON with Cyrillic, Excel with two mapping sheets)
- Decryption page works independently from masking flow
- Unknown values/columns pass through unchanged in decryption
- Statistics remain visible on Step 3 (OUT-04 unchanged from Phase 2)
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-decryption/03-02-SUMMARY.md`
</output>
