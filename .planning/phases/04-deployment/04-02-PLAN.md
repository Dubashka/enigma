---
phase: 04-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - INFRA-VERIFY
must_haves:
  truths:
    - "App is reachable at http://158.160.27.49 and shows the Enigma UI"
    - "App remains running after SSH disconnect (systemd keeps it alive)"
    - "Real test file with 4 sheets and ~30K rows processes without hanging in under 30 seconds"
    - "Two parallel browser sessions see only their own data"
    - "File >50MB uploads successfully (limit is 300MB)"
  artifacts:
    - path: "/etc/systemd/system/enigma.service"
      provides: "Active systemd service on VM"
      contains: "ExecStart=/home/enigma/venv/bin/streamlit"
    - path: "/etc/nginx/sites-enabled/enigma"
      provides: "Active nginx site config on VM"
      contains: "proxy_pass"
  key_links:
    - from: "browser at http://158.160.27.49"
      to: "nginx on VM port 80"
      via: "HTTP request"
      pattern: "curl.*158.160.27.49.*200"
    - from: "nginx on VM port 80"
      to: "streamlit on 127.0.0.1:8501"
      via: "proxy_pass with WebSocket upgrade"
      pattern: "proxy_pass.*127.0.0.1:8501"
---

<objective>
Deploy Enigma to VM and verify all 4 production success criteria from the roadmap.

Purpose: The app must be running in production, accessible by users, and handling real workloads. This plan covers the human-driven deployment and verification steps.
Output: Running production instance at http://158.160.27.49 with all success criteria confirmed.
</objective>

<execution_context>
@/Users/ilyamukha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ilyamukha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment/04-CONTEXT.md
@.planning/phases/04-deployment/04-RESEARCH.md
@.planning/phases/04-deployment/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy to VM using setup and deploy scripts</name>
  <files>deploy/setup-vm.sh, deploy/deploy.sh</files>
  <read_first>
    - deploy/setup-vm.sh (the script user will run)
    - deploy/deploy.sh (the script user will run after setup)
    - .planning/phases/04-deployment/04-RESEARCH.md (Open Questions section — check Python version, RAM, existing nginx)
  </read_first>
  <action>
User must SSH into the VM and deploy the application. Two scenarios:

**Scenario A: Fresh VM (first deploy)**
1. From the local project root, run: `bash deploy/setup-vm.sh`
   - This installs nginx, python3.11-venv, creates the `enigma` user, sets up the venv, installs systemd service, configures nginx, sets up sudoers
   - Requires root SSH access to 158.160.27.49
2. Then run: `bash deploy/deploy.sh`
   - This syncs code, installs pip deps, restarts the service

**Scenario B: Existing VM (update deploy)**
1. From the local project root, run: `bash deploy/deploy.sh`

**Before running setup-vm.sh, check:**
- SSH access works: `ssh root@158.160.27.49 "hostname"`
- Python version: `ssh root@158.160.27.49 "python3 --version"` (need 3.11+; if not available, install python3.11)
- Available RAM: `ssh root@158.160.27.49 "free -h"` (need at least 2GB free)

**After deploy.sh completes, verify service is running:**
```bash
ssh enigma@158.160.27.49 "sudo systemctl status enigma --no-pager"
```
Expected output: `Active: active (running)`
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://158.160.27.49/ | grep -q "200" && echo "PASS" || echo "FAIL — app not reachable at VM IP"</automated>
  </verify>
  <how-to-verify>
    1. Run `ssh enigma@158.160.27.49 "sudo systemctl status enigma --no-pager"` — should show `active (running)`
    2. Run `curl -s -o /dev/null -w "%{http_code}" http://158.160.27.49/` — should return `200`
    3. Open http://158.160.27.49 in browser — should show Enigma UI with Russian text
  </how-to-verify>
  <resume-signal>Type "deployed" when the app is running, or describe any errors encountered</resume-signal>
  <acceptance_criteria>
    - `systemctl is-active enigma` on VM returns `active`
    - `curl -s -o /dev/null -w "%{http_code}" http://158.160.27.49/` returns `200`
    - Browser at http://158.160.27.49 shows the Enigma upload page in Russian
  </acceptance_criteria>
  <done>App is deployed and accessible at http://158.160.27.49, systemd service is active, nginx is proxying correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production success criteria</name>
  <files>N/A — verification only, no files modified</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 Success Criteria — 4 items)
    - .planning/phases/04-deployment/04-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
Verify all 4 production success criteria from ROADMAP.md Phase 4. Each test must be performed manually by the user on the live VM at http://158.160.27.49.

**Test 1: App survives SSH disconnect**
1. SSH into VM: `ssh enigma@158.160.27.49`
2. Verify service: `sudo systemctl status enigma`
3. Disconnect SSH (close terminal)
4. Open http://158.160.27.49 in browser — app should still work
5. Reconnect SSH, run `sudo systemctl is-active enigma` — should say `active`

**Test 2: Real file performance (30K rows less than 30 seconds)**
1. Open http://158.160.27.49 in browser
2. Upload the test file "Данные для маскирования_13.03.xlsx" (4 sheets, ~30K rows)
3. Go through the masking flow (select columns, run masking)
4. Time the masking step — should complete in under 30 seconds
5. Download the masked file — should be a valid Excel file

**Test 3: Session isolation (two parallel sessions)**
1. Open http://158.160.27.49 in Chrome
2. Open http://158.160.27.49 in Firefox (or incognito window)
3. Upload different files in each browser
4. Verify each browser shows only its own file data
5. Run masking in one browser — the other should not be affected

**Test 4: File size limit behavior**
1. Upload a file smaller than 300 MB — should upload successfully
2. Attempt to upload a file larger than 300 MB — should show a readable error message (Streamlit UI error, not a raw nginx 413 page)
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://158.160.27.49/ | grep -q "200" && echo "PASS — app still reachable" || echo "FAIL — app not reachable"</automated>
  </verify>
  <what-built>Complete production deployment of Enigma on VM 158.160.27.49</what-built>
  <how-to-verify>
Run all 4 tests described in the action section above. If any test fails, describe the failure. If all pass, type "all 4 tests pass".
  </how-to-verify>
  <resume-signal>Type "all 4 tests pass" or describe which tests failed and how</resume-signal>
  <acceptance_criteria>
    - Test 1: `systemctl is-active enigma` returns `active` after SSH disconnect and reconnect
    - Test 2: Masking of 4-sheet ~30K-row file completes in under 30 seconds
    - Test 3: Two simultaneous browser sessions show independent data
    - Test 4: Files under 300MB upload; files over 300MB show an error message
  </acceptance_criteria>
  <done>All 4 roadmap success criteria for Phase 4 are verified on the production VM. The app handles real data, survives disconnects, isolates sessions, and enforces file size limits.</done>
</task>

</tasks>

<verification>
All 4 Phase 4 success criteria from ROADMAP.md are manually verified:
1. App accessible at VM IP, stays running after SSH disconnect
2. 30K-row file processes under 30 seconds
3. Parallel sessions are isolated
4. File size limits produce readable errors
</verification>

<success_criteria>
1. Enigma is deployed and running at http://158.160.27.49
2. systemd service survives SSH disconnect and auto-restarts on crash
3. Real test file processes in under 30 seconds
4. Two parallel browser sessions are fully isolated
5. File upload limits work correctly (300MB allowed, >300MB rejected with readable error)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-02-SUMMARY.md`
</output>
